// lib/encryption.ts

// Bypass encryption and decryption for demo purposes
export const encrypt = (text: string): string => {
    return text; // No encryption in demo
  };
  
  export const decrypt = (text: string): string => {
    return text; // No decryption in demo
  };
  